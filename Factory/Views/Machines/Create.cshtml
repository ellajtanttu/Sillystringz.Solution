@{
  Layout = "_Layout";
}

@model FactoryManager.Models.Machine

<h1>Add a new machine: </h1>

@using (Html.BeginForm())
{
  @Html.Label("Select Engineer")
  @Html.DropDownList("EngineerId")

  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name,
  new {required="required"})

  <input type="submit" value="Add new machine" class="btn btn-lg btn-dark"/>
}

<p>@Html.ActionLink("return to machine page", "Index")</p>
<p>@Html.ActionLink("Return to homepage", "Index", "Home")</p>